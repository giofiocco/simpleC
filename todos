when ast_malloc a group return the ast of the expr

tokenizer check if it is bin

tokenizer maybe find the row end when changing the row instead of finding it when printing error

escape char in string

typecheck maybe check if code is if or something with a block and then call typecheck_funcbody on it
typecheck warn if no return in some function

parser maybe code and param ecc return a ast with {NULL, NULL} for the forerror
